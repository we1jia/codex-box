# Codex Box · 数据模型 v0.3

> BYOK 模型下拉与多 provider 路由
> 状态:v0.3 基线 · 2026-06-25
> 替代:[docs/data-model/v0.2-archived-20260625.md](./v0.2-archived-20260625.md)(v0.2 的 GatewayRuntime / GatewayAccess / ExternalOpenCodexCheckout 等模型基于错的项目定性,已废弃)
> 对应:[PRD.md §7](../../PRD.md) · [docs/architecture/v0.3-BYOK.md](../architecture/v0.3-BYOK.md) · [docs/design/v0.1.md](../design/v0.1.md)

---

## 1. 总览

Codex Box v0.3 围绕三组模型构建:

1. **Codex 配置模型**:`CodexConfigSnapshot`、`CodexProfile`、`ModelProvider`、`McpServer`、`NetworkRoute`
2. **BYOK 模型目录模型**:`ModelCatalogEntry`、`ProviderRoute`、`CodexBoxCustomConfig`
3. **运行时 / 安全写入 / 诊断模型**:`CodexRuntimeStatus`、`ConfigChangePreview`、`BackupRecord`、`HealthStatus`、`SecretRef`

```
CodexConfigSnapshot
  ├─ CodexProfile
  ├─ ModelProvider
  ├─ McpServer
  └─ NetworkRoute

CodexBoxCustomConfig
  ├─ ProviderRoute[]
  └─ ModelCatalogEntry[]

ConfigChangePreview
  ├─ BackupRecord
  ├─ HealthStatus
  └─ SecretRef
```

---

## 2. CodexConfigSnapshot

```rust
struct CodexConfigSnapshot {
    path: PathBuf,                    // ~/.codex/config.toml
    read_at: DateTime<Utc>,
    raw_text: String,
    parsed: ParsedConfig,
    content_hash: String,
    valid: bool,
    parse_errors: Vec<ParseError>,
}

struct ParsedConfig {
    active_profile: Option<String>,
    profiles: Vec<CodexProfile>,
    model_providers: Vec<ModelProvider>,
    mcp_servers: Vec<McpServer>,
    default_network: Option<String>,
    network_routes: Vec<NetworkRoute>,
    top_level: HashMap<String, TomlValue>,
    other_tables: HashMap<String, TomlValue>,
}
```

要求:

- 未识别字段必须保留,后续写入不能无意删除。
- `raw_text` 只用于 diff 和回滚,不进入日志。
- `content_hash` 用于防止并发覆盖。

---

## 3. CodexProfile

```rust
struct CodexProfile {
    name: String,
    model_provider: Option<String>,
    model: Option<String>,
    sandbox: Option<SandboxMode>,
    approval: Option<ApprovalMode>,
    features: Vec<String>,
    env_refs: Vec<SecretRef>,
    mcp_refs: Vec<String>,
    network: Option<String>,
    is_active: bool,
    is_default: bool,
    note: Option<String>,
}
```

校验规则:

- `model_provider` 存在时必须能在 `model_providers` 中找到。
- `mcp_refs` 中每个 name 必须能在 `mcp_servers` 中找到。
- `network` 存在时必须能在 `network_routes` 中找到。
- 切换模型时切换的是完整 profile,不是只改裸 `model` 字段。
- 首次新增第三方 provider 时,需要保留官方订阅 profile。

---

## 4. ModelProvider(`~/.codex/config.toml` 内)

```rust
struct ModelProvider {
    name: String,
    kind: ProviderKind,
    channel: ProviderChannel,
    base_url: Option<String>,
    wire_api: WireApi,
    request_timeout: Option<u32>,
    api_key_ref: Option<SecretRef>,
    http_headers: HashMap<String, String>,
    models: Vec<String>,                // 该 provider 下的模型 id 列表
    is_active: bool,
    note: Option<String>,
}

enum ProviderKind {
    OpenAISubscription,
    OpenAIOfficialApi,
    OpenAICompatibleApi,
    LocalGateway,
}

enum ProviderChannel {
    Subscription,                       // 官方订阅,不读 token
    Api,                                // 走 env 引用 key
    Gateway,                            // 走本地/中间层 endpoint
}

enum WireApi {
    Chat,
    Responses,
    SseStream,
    Custom(String),
}
```

`WireApi` 路由语义：

- `Responses`：上游本身支持 `/v1/responses` 时优先 passthrough，Codex Box 只做 model 改写、鉴权注入和必要的最小 normalize（例如过滤 `<think>...</think>` 污染）。
- `Chat`：上游只支持 `/v1/chat/completions` 时，Codex Box 才把 Codex Desktop 的 Responses 请求转换为 Chat，再把 Chat SSE 翻译回 Responses events。
- 这不是“OpenCodex 代理一定比直连更差”，而是兼容层策略：能原生 Responses 就少转换，不能原生 Responses 才转换。

校验规则:

- `channel = Api` 时必须提供 `api_key_ref`。
- `channel = Subscription` 时禁止配置 `api_key_ref`,也禁止读取或复用官方账号 token。
- `channel = Gateway` 时 `base_url` 必须指向本地或显式配置的 endpoint。
- secret 只允许通过 env 或 Keychain 引用,不写明文。

---

## 5. ProviderRoute(`~/.codex/codex-box/providers.json` 条目)

```rust
struct ProviderRoute {
    name: String,                       // 唯一 name,与 ModelProvider.name 对应
    base_url: String,
    wire_api: WireApi,
    api_key: Option<String>,            // 文件字段;Codex Box 只允许 "$ENV_VAR" 或 "${ENV_VAR}"
    http_headers: HashMap<String, String>,
    enabled: bool,                      // 是否在 Codex App 模型下拉里可见
    note: Option<String>,
    codex_routing: Option<CodexRoutingConfig>,
    // 未知字段保留,写入时原样回写
}

struct CodexRoutingConfig {
    enabled: Option<bool>,              // Some(false) 时禁用;省略且 routes 非空时按启用处理
    default_route_id: Option<String>,
    routes: Vec<CodexRoutingRoute>,
}

struct CodexRoutingRoute {
    id: String,
    label: Option<String>,
    enabled: Option<bool>,              // 省略按 true
    target_provider_id: Option<String>, // 指向同一 providers.json 内真实上游 provider
    match: CodexRoutingMatch,
    upstream: CodexRoutingUpstream,
}

struct CodexRoutingMatch {
    models: Vec<String>,                // 精确匹配 Codex picker 里的 model id
    prefixes: Vec<String>,              // 前缀匹配,如 "gpt-" / "qwen-"
}

struct CodexRoutingUpstream {
    base_url: Option<String>,           // 可覆盖 target provider 的 base_url
    api_format: Option<String>,         // openai_responses/openai_chat/openai_messages
    auth: Option<AuthBinding>,          // provider_config / managed_codex_oauth 等
    api_key: Option<String>,            // "$ENV_VAR" / "${ENV_VAR}" 或内存直传;写日志必须脱敏
    model_map: HashMap<String, String>, // picker model id → 上游真实 model id
}
```

要求:

- 文件不存在时按"空配置"处理,不要报错。
- 未知字段保留并回写,不删字段。
- 文件字段名保持 OpenCodex 兼容的 `api_key`,Codex Box UI 内部可映射为 `api_key_ref`。
- 写回文件时只允许 `$ENV_VAR` 或 `${ENV_VAR}` 引用,不接受 inline value。
- 优先写 `{ "providers": [...] }` envelope,不是顶层裸数组。
- `codexRouting` 用于 CCSwitchMulti/OpenCodex MultiRouter 形态:Codex App 只连接一个本地 router provider,下拉框显示聚合 catalog;请求进入代理后按 `match.models/prefixes/defaultRouteId` 选 route,再转到 `targetProviderId` 对应的真实 provider。
- 官方订阅 GPT/Codex 模型进入 MultiRouter 时使用 `auth.source = "managed_codex_oauth"` 的 `openai-official` route。Codex Box 当前只透传 Codex Desktop 本次请求携带的官方认证上下文,不读取或落盘官方账号 token。
- 诊断状态会单独检查 `openai-official` route 是否存在、`auth.source` 是否为 `managed_codex_oauth`、`baseUrl` 是否指向 `https://chatgpt.com/backend-api/codex`,避免官方登录态问题和路由配置问题互相掩盖。
- `apiFormat=openai_responses` 时优先走 `/responses` passthrough/minimal normalize;`apiFormat=openai_chat` 时走 Responses ↔ Chat translator。
- Codex Box 写这份 JSON 时走与 `config.toml` 相同的 backup → diff → confirm → atomic write → rollback 闭环。

---

## 6. ModelCatalogEntry(`~/.codex/codex-box/custom_model_catalog.json` 条目)

```rust
struct ModelCatalogEntry {
    slug: String,                       // Codex App picker 看到的 model id
    model: String,                      // 通常等于 slug
    display_name: Option<String>,       // 友好显示名
    provider: String,                   // 归属 provider name
    backend_model: Option<String>,      // 转发给上游 API 的真实 model
    backend_provider: Option<String>,   // 路由到哪个 providers.json provider
    target_provider: Option<String>,    // MultiRouter 下的人类可读真实上游 provider
    visibility: String,                 // "list" 才进入 /v1/models
    reasoning: Option<ReasoningConfig>,
    note: Option<String>,
    // 未知字段保留
}

struct ReasoningConfig {
    enabled: bool,
    levels: Vec<String>,                // e.g. ["low", "medium", "high"]
}
```

要求:

- 文件字段名保持 OpenCodex 兼容的 `slug` / `backend_model` / `backend_provider` / `visibility`。
- Codex Box UI 内部可把 `slug` 映射成 `model_id`,把 `visibility == "list"` 映射成 `visible=true`,但写回文件必须恢复 AITabby 字段。
- MultiRouter apply 后,第三方模型的 `backend_provider` 可以指向稳定本地 router provider(如 `codex_model_router_v2`),同时用 `target_provider` / `targetProvider` 保留真实上游服务商(如 `minimax`)供 UI、`/v1/models` 元数据和诊断展示。再次 preview/apply 时优先用该字段恢复真实上游,再退回旧 router route 推断。真实请求仍以 `codexRouting.routes[].targetProviderId + model_map` 为准。
- `/v1/models` 返回给 Codex App 的 `id` 默认使用 `slug` 本身。
- `/v1/models` 投影 `backendProvider` 时优先展示 `targetProvider`,并在二者不同时额外投影 `routerProvider`,避免用户把本地 router 误认为真实模型商。
- `/v1/responses` / `/v1/chat/completions` 转发前必须把请求体 `model` 改写为 `backend_model` 或 `model`,不能把 picker 展示用 `slug` 原样发给上游。
- 同 `ProviderRoute` — 未知字段保留、空文件按空处理、secret 走 env 引用。

---

## 7. CodexBoxCustomConfig(`~/.codex/codex-box/` 完整快照)

```rust
struct CodexBoxCustomConfig {
    schema_version: u32,                 // 当前固定 1
    providers_path: PathBuf,             // ~/.codex/codex-box/providers.json
    catalog_path: PathBuf,               // ~/.codex/codex-box/custom_model_catalog.json
    legacy_opencodex_providers_path: Option<PathBuf>,
    legacy_opencodex_catalog_path: Option<PathBuf>,
    providers: Vec<ProviderRoute>,
    catalog: Vec<ModelCatalogEntry>,
    raw_providers_text: String,          // 用于 diff / rollback
    raw_catalog_text: String,
    content_hash: String,
    read_at: DateTime<Utc>,
    valid: bool,
    parse_errors: Vec<ParseError>,
}
```

---

## 8. CodexRuntimeStatus(只读)

```rust
struct CodexRuntimeStatus {
    codex_home: PathBuf,                // ~/.codex
    codex_cli_path: Option<PathBuf>,
    codex_desktop_app_path: Option<PathBuf>,
    codex_desktop_version: Option<String>,
    desktop_installed: bool,
    cli_available: bool,
    config_readable: bool,
    auth_state_detected: bool,
    // 只读检测,绝不修改 Codex Desktop
}

struct OfficialResourceScan {
    enabled: bool,
    readonly: bool,
    app_asar_path: Option<PathBuf>,
    cache_dir: Option<PathBuf>,
    last_scan_error: Option<String>,
}
```

要求:

- 默认只做安装检测和只读状态识别。
- 不默认解包 `app.asar`。
- 不修改 Codex Desktop 内部文件。

---

## 9. McpServer

```rust
struct McpServer {
    name: String,
    command: Option<String>,
    args: Vec<String>,
    url: Option<String>,
    env: HashMap<String, String>,
    enabled: bool,
    last_health: Option<HealthStatus>,
    last_error: Option<String>,
}
```

校验规则:

- stdio server 的 `command` 必须在 `$PATH` 中可执行。
- HTTP server 的 `url` 必须可解析。
- env 中疑似 secret 字段必须脱敏。

---

## 10. NetworkRoute

```rust
struct NetworkRoute {
    name: String,
    kind: NetworkKind,
    proxy_url: Option<String>,
    proxy_auth: Option<ProxyAuth>,
    no_proxy: Vec<String>,
    is_active: bool,
}
```

要求:

- 不设置系统全局代理。
- 只影响 Codex Box 自身请求或显式启动的 gateway runtime。

---

## 11. ConfigChangePreview

```rust
struct ConfigChangePreview {
    target: ConfigTarget,               // ~/.codex/config.toml 或 ~/.codex/codex-box/*.json
    operation: ConfigOperation,
    current_hash: String,
    proposed_text: String,
    diff_lines: Vec<DiffLine>,
    warnings: Vec<String>,
}

enum ConfigTarget {
    CodexConfig,                        // ~/.codex/config.toml
    CodexBoxProviders,                  // ~/.codex/codex-box/providers.json
    CodexBoxCatalog,                    // ~/.codex/codex-box/custom_model_catalog.json
    ImportSource,                       // 第三方只读导入来源
}

enum ConfigOperation {
    SetActiveProfile { name: String },
    CreateProfile(CodexProfile),
    UpdateProfile(CodexProfile),
    DeleteProfile { name: String },
    CreateProvider(ModelProvider),
    UpdateProvider(ModelProvider),
    DeleteProvider { name: String },
    ToggleModelVisibility { slug: String, visibility: String },
    UpsertProviderRoute(ProviderRoute),
    DeleteProviderRoute { name: String },
    UpsertCatalogEntry(ModelCatalogEntry),
    DeleteCatalogEntry { slug: String },
    RollbackBackup { backup_id: String },
}
```

写入链路:

1. 读取当前文件
2. 校验 `content_hash`
3. 生成 preview 和 diff
4. 用户确认
5. 创建 backup
6. 写 `.tmp`
7. `rename`
8. 失败则 rollback

---

## 12. BackupRecord

```rust
struct BackupRecord {
    id: String,
    created_at: DateTime<Utc>,
    file_path: PathBuf,                  // 备份的是哪个文件
    reason: BackupReason,
    content_hash: String,
    size_bytes: u64,
    note: Option<String>,
}
```

存储路径:`~/.codex/codex-box/backups/{timestamp}-{hash}.toml` 或 `.json`(按被备份文件扩展名)

---

## 13. HealthStatus

```rust
struct HealthStatus {
    check: HealthCheck,
    status: CheckStatus,
    latency_ms: Option<u64>,
    message: Option<String>,
    checked_at: DateTime<Utc>,
}

enum HealthCheck {
    ConfigSyntax,
    CodexDesktopInstalled,
    CodexCliAvailable,
    CodexHomeReadable,
    CodexBoxProvidersValid,             // ~/.codex/codex-box/providers.json 解析
    CodexBoxCatalogValid,               // ~/.codex/codex-box/custom_model_catalog.json 解析
    ImportSourceDetected,               // OpenCodex / Codex++ / CC Switch / Cockpit Tools 兼容来源
    ProviderUrlReachable,               // 任意 provider.base_url HEAD probe
    ProviderEnvExists,                  // 任意 provider.api_key_ref 对应 env 是否存在
    McpServer,
    BackupSpace,
}
```

---

## 14. SecretRef

```rust
struct SecretRef {
    source: SecretSource,
    ref_id: String,                      // e.g. "OPENAI_API_KEY"
    display: String,                     // UI 脱敏显示,如 "OPENAI_API_KEY (configured)"
}

enum SecretSource {
    Env,                                // ${ENV_VAR}
    Keychain,
    InlineMemoryOnly,                   // 仅进程内存,不允许写入文件
}
```

红线:

- `InlineMemoryOnly` 只允许存在于当前进程内存。
- 真实 secret 不写日志、不写诊断报告、不写 JSON 导出。
- 写入 config 时只写 `${ENV_VAR}` 或等价引用,不写明文。
- 写入 `~/.codex/codex-box/*.json` 时同上。

---

## 15. 持久化路径

| 内容 | 路径 | 谁写 |
|---|---|---|
| 主 config | `~/.codex/config.toml` | Codex App 自己(Codex Box 写它) |
| Codex Box 自有配置 | `~/.codex/codex-box/config.json` | Codex Box |
| 备份 | `~/.codex/codex-box/backups/{ts}-{hash}.{toml,json}` | Codex Box |
| 日志 | `~/.codex/codex-box/logs/{date}.log` | Codex Box |
| 诊断报告 | `~/.codex/codex-box/reports/{date}.md` | Codex Box |
| Provider 路由 | `~/.codex/codex-box/providers.json` | Codex Box |
| 自定义模型目录 | `~/.codex/codex-box/custom_model_catalog.json` | Codex Box |
| OpenCodex 导入来源 | `~/.opencodex/providers.json` / `~/.opencodex/custom_model_catalog.json` | 只读扫描,用户确认后导入 |

---

## 16. 与 v0.2 数据模型的关键差异

| v0.2 | v0.3 | 原因 |
|---|---|---|
| `GatewayRuntime` / `GatewayAccess` | 删除 | 项目不再起外部 gateway 进程 |
| `GatewaySource::ExternalOpenCodexCheckout` | 删除 | 不再 spawn 外部 OpenCodex |
| `~/.codex/codex-box/opencodex/runtime/` | 删除 | 没有"外部 OpenCodex runtime"概念 |
| 无 | `ProviderRoute`(`~/.codex/codex-box/providers.json` 条目) | BYOK 多 provider 路由核心模型 |
| 无 | `ModelCatalogEntry`(`~/.codex/codex-box/custom_model_catalog.json` 条目) | BYOK 自定义模型目录 |
| 无 | `CodexBoxCustomConfig`(`~/.codex/codex-box/` 完整快照) | 让前端能一次拿全两份 JSON |
| 无 | `ConfigTarget` 枚举 | 写入闭环对多个文件都生效 |
| 无 | `HealthCheck::CodexBoxProvidersValid` / `CodexBoxCatalogValid` | 诊断新加的 JSON 完整性检查 |
| 无 | `HealthCheck::ProviderUrlReachable` / `ProviderEnvExists` | 诊断 provider 可用性 |

---

## 版本

- v0.3 · 2026-06-25 · BYOK 模型下拉与多 provider 路由数据模型
